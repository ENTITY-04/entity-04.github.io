# Images Folder

Add your cat photos here and update the `src` attributes in `index.html` to point to the local files (for example `images/luna-window.jpg`). Keeping this README prevents Git from ignoring the directory.
